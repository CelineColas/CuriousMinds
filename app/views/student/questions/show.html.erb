<!-- <div class="flip">

  <div class="card">

    <div class="face front">
      <%= @question.title %>
    </div>


    <div class="face back">


      <div class="question-input-field">
      <%= simple_form_for [:student, @quest, @question, @effort] do |f| %>
      <%= f.input :content %>
      <%= f.button :submit, class: "btn btn-success" %>
      <% end %>
      </div>
      <div class="wrapper">

      </div>
  </div>
</div>
</div> -->


<div class="card">
  <div class="card-front">
    <div class="clickable-surface">
      <h1><%= @question.title %></h1>
    </div>
  </div>
  <div class="card-back">
    <div class="card-input">
      <%= simple_form_for [:student, @quest, @question, @effort] do |f| %>
      <%= f.input :content %>
      <%= f.button :submit, class: "btn btn-success" %>
      <% end %>
    </div>
    <div class="clickable-surface">

    </div>
  </div>
</div>
